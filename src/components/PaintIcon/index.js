import React from "react";
import paint from "./paint.svg";

function PaintIcon() {
  return (
    <div>
      <img style={{ height: "400px" }} src={paint} />
    </div>
  );
}

export default PaintIcon;
